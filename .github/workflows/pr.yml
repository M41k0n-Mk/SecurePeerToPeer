name: PR Checks

on:
  pull_request:
    branches: [ main, develop ]

jobs:
  test-and-scan:
    name: PR Build & Test (JDK ${{ matrix.java }})
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    strategy:
      matrix:
        java: [ '17', '21' ]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
          distribution: 'temurin'
          cache: maven

      - name: Cache OWASP Dependency-Check data
        uses: actions/cache@v5
        with:
          path: ~/.m2/repository/org/owasp/dependency-check-data
          key: owasp-nvd-${{ runner.os }}-${{ hashFiles('**/pom.xml') }}
          restore-keys: owasp-nvd-${{ runner.os }}-

      - name: Build, unit and integration tests
        run: mvn -B -e clean verify

      - name: Lint (Checkstyle)
        run: mvn -B checkstyle:check

      - name: Static analysis (SpotBugs)
        run: mvn -B spotbugs:spotbugs spotbugs:check || true

      - name: Security scan (OWASP Dependency-Check)
        run: mvn -B org.owasp:dependency-check-maven:aggregate -Dformat=HTML -Dformats=HTML,XML,SARIF -DoutputDirectory=target || true

      - name: Check SARIF exists
        id: sarifcheck
        if: always()
        run: |
          if [ -f target/dependency-check-report.sarif ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Upload SARIF (OWASP)
        if: ${{ steps.sarifcheck.outputs.exists == 'true' }}
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: target/dependency-check-report.sarif

      - name: Upload reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pr-reports-jdk${{ matrix.java }}
          path: |
            target/site/**
            target/dependency-check-report.*
            target/spotbugsXml.xml
            target/spotbugsHtml.html
